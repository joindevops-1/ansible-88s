- name: conditions on demo
  hosts: local
  connection: local
  vars:
    number_of_sessions: 6
    IS_LIVE: false
  tasks:
  - name: more Sessions
    ansible.builtin.debug:
      msg: "Yes, more sessions"
    when: 
    - number_of_sessions > 5 or IS_LIVE